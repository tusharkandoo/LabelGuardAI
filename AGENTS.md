# Project Guidelines

LabelGuard AI is an AI-assisted legal metrology compliance inspection platform for packaged commodities.

### Development Guidelines

- Always verify changes with `npm run build` and `npm run dev`.
- Ensure environment variables for AI vision models (`GEMINI_API_KEY`, `OPENAI_API_KEY`) are kept secure in `.env`.
